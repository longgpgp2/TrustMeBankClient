export default function SavingsTableHead() {
    return (
        <thead>
            <tr>
                <th class="py-2 px-4 border-b max-w-xs truncate">ID</th>
                <th class="py-2 px-4 border-b max-w-xs truncate">Saver</th>
                <th class="py-2 px-4 border-b max-w-xs truncate">Saving Name</th>
                <th class="py-2 px-4 border-b max-w-xs truncate">Saving Type</th>
                <th class="py-2 px-4 border-b max-w-xs truncate">Amount</th>
                <th class="py-2 px-4 border-b max-w-xs truncate">Interest Rate</th>
                <th class="py-2 px-4 border-b max-w-xs truncate">Request Date</th>
                <th class="py-2 px-4 border-b max-w-xs truncate">Start Date</th>
                <th class="py-2 px-4 border-b max-w-xs truncate">End Date</th>
                <th class="py-2 px-4 border-b max-w-xs truncate">Status</th>
            </tr>
        </thead>
    )
}